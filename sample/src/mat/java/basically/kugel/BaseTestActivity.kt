package basically.kugel

import androidx.appcompat.app.AppCompatActivity

/**
 * MAT flavor: AppCompatActivity + Theme.AppCompat — "material without
 * Material3". (AppCompatActivity cannot run framework themes.)
 */
open class BaseTestActivity : AppCompatActivity()
